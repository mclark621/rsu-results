rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default-deny. Open access only for the collections you explicitly add.
    allow read, write: if false;
  }
}
